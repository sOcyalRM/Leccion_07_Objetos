//Todo esto puede ser parte de los elementos de un objeto
const nombre = "Monitor de 20 Pulgadas";
const precio = 300;
const disponible = true;

//Un objeto agrupa todo en una variable

//Lo siguiente se conoce como un 'object literal'
const producto = {
    nombre : "Monitor de 20 Pulgadas", //No se utiliza (=) para asignar valores sino (:)
    precio : 300, //Los elementos se separan con (,) en lugar de (;)
    disponible : true,
}

console.log (producto);